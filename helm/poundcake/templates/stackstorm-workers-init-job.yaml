{{- if and .Values.stackstorm.chart.enabled .Values.stackstorm.startupOrder.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "poundcake.fullname" . }}-stackstorm-workers-init
  labels:
    {{- include "poundcake.labels" . | nindent 4 }}
    app.kubernetes.io/component: stackstorm-workers-init
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "11"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        {{- include "poundcake.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: stackstorm-workers-init
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "poundcake.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
      - name: stackstorm-workers-init
        image: "bitnami/kubectl:1.30"
        imagePullPolicy: IfNotPresent
        command:
        - /bin/sh
        - -c
        - |
          set -euo pipefail

          NS="{{ .Release.Namespace }}"
          RELEASE_NAME="{{ .Release.Name }}"
          ST2_PREFIX="{{ include "poundcake.stackstormSubchartPrefix" . }}"
          TIMEOUT=600

          if ! command -v kubectl >/dev/null 2>&1; then
            echo "ERROR: kubectl not found"
            exit 1
          fi

          resolve_deployment_name() {
            component="$1"
            for candidate in "${ST2_PREFIX}-${component}" "${RELEASE_NAME}-${component}" "${component}"; do
              if kubectl -n "${NS}" get deployment "${candidate}" >/dev/null 2>&1; then
                echo "${candidate}"
                return 0
              fi
            done
            kubectl -n "${NS}" get deployment -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}' \
              | grep -E "(-|^)${component}$" | head -n1 || true
          }

          scale_component_if_exists() {
            component="$1"
            replicas="$2"
            name="$(resolve_deployment_name "${component}")"
            if [ -n "${name}" ]; then
              kubectl -n "${NS}" scale deployment "${name}" --replicas="${replicas}" >/dev/null
              echo "Scaled ${name} -> ${replicas}"
            fi
          }

          wait_component_rollout_if_exists() {
            component="$1"
            name="$(resolve_deployment_name "${component}")"
            if [ -n "${name}" ]; then
              kubectl -n "${NS}" rollout status "deployment/${name}" --timeout="${TIMEOUT}s"
            fi
          }

          echo "Stage 5: scaling StackStorm worker/execution components."
          scale_component_if_exists "st2actionrunner" {{ .Values.stackstorm.startupOrder.workerReplicas.st2actionrunner | int }}
          scale_component_if_exists "st2rulesengine" {{ .Values.stackstorm.startupOrder.workerReplicas.st2rulesengine | int }}
          scale_component_if_exists "st2workflowengine" {{ .Values.stackstorm.startupOrder.workerReplicas.st2workflowengine | int }}
          scale_component_if_exists "st2scheduler" {{ .Values.stackstorm.startupOrder.workerReplicas.st2scheduler | int }}
          scale_component_if_exists "st2notifier" {{ .Values.stackstorm.startupOrder.workerReplicas.st2notifier | int }}
          scale_component_if_exists "st2garbagecollector" {{ .Values.stackstorm.startupOrder.workerReplicas.st2garbagecollector | int }}
          scale_component_if_exists "st2timersengine" {{ .Values.stackstorm.startupOrder.workerReplicas.st2timersengine | int }}
          scale_component_if_exists "st2chatops" {{ .Values.stackstorm.startupOrder.workerReplicas.st2chatops | int }}

          wait_component_rollout_if_exists "st2actionrunner"
          wait_component_rollout_if_exists "st2rulesengine"
          wait_component_rollout_if_exists "st2workflowengine"
          wait_component_rollout_if_exists "st2scheduler"
          wait_component_rollout_if_exists "st2notifier"
          wait_component_rollout_if_exists "st2garbagecollector"
          wait_component_rollout_if_exists "st2timersengine"
{{- end }}
